<%= form_for([@pin, @pin.comments.build]) do |f| %>
	<p>
		<%= f.label :name %>
    <%= f.text_area :name %>

	</p>
	<p>
		<%= f.label :body %> <br>
		<%= f.text_area :body %>
	</p>
	<br>
	<p>
		<%= f.submit %>
	</p>
<% end %>
